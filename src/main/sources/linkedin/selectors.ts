/**
 * LinkedIn DOM selectors - re-exported from browser/selectors.ts for convenience.
 * The actual selectors live in the shared browser module since the extractor uses them.
 */

export { SELECTORS } from '../../browser/selectors'
